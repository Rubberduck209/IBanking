# Copy file này thành ".env" và điền giá trị thật, KHÔNG commit file .env lên git

# --- Database ---
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=12345678
DB_NAME=payment_DB

# --- JWT ---
# Sinh chuỗi random bằng: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=thay_bang_chuoi_random_it_nhat_32_ky_tu
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
